/* esp_chip_info.h — host stub of IDF's esp_chip_info.h.
 *
 * The IDF real header is at $IDF_PATH/components/esp_hw_support/include/
 * but its inclusion chain pulls in many device-only headers. On host the
 * `esp_chip_info_t` typedef comes from mock_esp_system.h, included
 * earlier in the production source. */
#ifndef HOST_ESP_CHIP_INFO_H
#define HOST_ESP_CHIP_INFO_H

#endif /* HOST_ESP_CHIP_INFO_H */
