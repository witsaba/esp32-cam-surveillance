# ESP-IDF project root — firmware (FW-02 milestone)
#
# This file is generated by `idf.py create-project` (or hand-authored for
# the bootstrap commit; both produce equivalent results). `idf.py` is the
# only supported way to interact with the build system from here on.
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(firmware)